<h1>Новая дисциплина</h1>

<%= form_with(model: @discipline, local: true) do |form| %>
    <div>
      <%= form.label :name, 'Название дисциплины' %>
      <%= form.text_field :name %>
    </div>
    
    <div>
      <%= form.submit 'Сохранить' %>
    </div>
  <% end %>

<%= link_to 'Назад к списку', disciplines_path %>